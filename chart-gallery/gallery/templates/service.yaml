apiVersion: v1
kind: Service
metadata:
  name: {{ include "gallery.fullname" . }}
  labels:
    {{- include "gallery.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gallery.selectorLabels" . | nindent 4 }}
