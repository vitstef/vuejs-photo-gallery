apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gallery.fullname" . }}-test-connection"
  labels:
    {{- include "gallery.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gallery.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
